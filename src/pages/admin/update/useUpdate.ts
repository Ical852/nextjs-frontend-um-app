import { UpdateAdminPageProps } from "@/types";

export const useUpdate = (props: UpdateAdminPageProps) => {
  const {
    updateAdmin,
    updateAdminLoading,
    updateAdminError,
    updateAdminResponse,
    updateAdminReset,

    session,
  } = props;

  return {};
};
