import { UpdateCategoryPageProps } from "@/types/modules/category";

export const useUpdate = (props: UpdateCategoryPageProps) => {
  const {
    updateCategory,
    updateCategoryLoading,
    updateCategoryError,
    updateCategoryResponse,
    updateCategoryReset,
  } = props;

  return {};
};
