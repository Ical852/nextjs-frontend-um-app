import { CreateCategoryPageProps } from "@/types/modules/category";

export const useCreate = (props: CreateCategoryPageProps) => {
  const {
    createCategory,
    createCategoryLoading,
    createCategoryError,
    createCategoryResponse,
    createCategoryReset,
  } = props;

  return {};
};
